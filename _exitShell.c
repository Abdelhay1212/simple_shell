#include "shell.h"

/**
 * _exit - exit with error number
 * Return: void
 **/
void _exitShell()
{
	/*int err;

	if (tokens[1] != NULL)
	{
		err = atoi(tokens[1]);
		exit(err);
	}
	else
		exit(0);*/
	exit(0);
}

